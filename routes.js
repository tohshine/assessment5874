const routes = require("next-routes")();

//routes
  //.add("/", "/pages")
  // .add("/events/fund", "/events/fund")
  // .add("/events/404", "/events/404")
  // .add("/connection/database", "/connection/database")
  // .add("/services/verify", "/services/verify")

  // .add("/events/show/:address", "/events/show")
  // .add("/events/:address/:index/:blockaddress", "/events/scan")

module.exports = routes;
